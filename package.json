{
  "name": "waschplan-app",
  "version": "0.1.0",
  "description": "Waschraum-Buchungsapp mit Express, SQLite und statischem Frontend.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "verify": "node --check server.js && node --check swiss-time.js && node --check release-window.js && node --check public/app.js && node --check public/login.js && node --check public/reset.js && node --check scripts/app-test.js && node --check scripts/role-matrix-test.js && node --check scripts/year-simulation-test.js && node --check scripts/accessibility-check.js",
    "test": "node scripts/app-test.js",
    "test:roles": "node scripts/role-matrix-test.js",
    "test:year": "node scripts/year-simulation-test.js",
    "test:a11y": "node scripts/accessibility-check.js",
    "check": "npm run verify && npm test && npm run test:roles && npm run test:year && npm run test:a11y"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "bcryptjs": "^3.0.2",
    "express": "^4.21.2",
    "express-session": "^1.18.1"
  }
}
